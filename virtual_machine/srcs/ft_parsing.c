#include "../includes/op.h"
#include "../../lem_in/libft/includes/libft.h"
#include "struct.h"

int		ft_parse(int nb_args, char **args)
{
	//if (nb_args > 0)
	//	ft_option();
	
	return (EXIT_SUCCESS);
}

int		main(int argc, char **argv)
{
	ft_parse(argc - 2, &argv[1]);
	return (EXIT_SUCCESS);
}
